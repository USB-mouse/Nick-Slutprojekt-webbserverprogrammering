{% extends "base.html.twig" %}

{% block main %}

 <table>
{% for item in movies %}
        <tr>
          <td> <a href="{{url("info.php")}}?MovieId={{ item.MovieId }}&genre={{item.Genre}}">{{ item.Title }}</a></td>
        </tr>
  {% endfor %}
    </table>


{% endblock %}